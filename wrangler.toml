name = "livepricetrackingbot"
main = "index.js"
compatibility_date = "2024-08-10"
compatibility_flags = ["nodejs_compat"]

# Remove env.production unless you specifically use multiple environments
# Only keep the main configuration above for simplicity